worker_processes 1;
error_log /dev/null;
pid /tmp/nginx.pid;

events { worker_connections 512; }

http {
    access_log off;
    server {
        listen 8080;
        location / {
            return 200 'ok';
            add_header Content-Type text/plain;
        }
    }
}
